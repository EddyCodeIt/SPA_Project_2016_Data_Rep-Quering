from app import app 
# from module app import attribute app

app.run(debug = True) 
# run app
    
    
    # with params app.run(port=port, host=host)
    # port = port number (use int() parser for port number in String)
    # host = host number (IP address)
